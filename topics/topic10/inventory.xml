<?xml version="1.0"?>

<!-- Для атрибута StockCode определен маркерный тип с использованием ключевого слова ID.
(ID - это только одно из ключевых слов, которые можно использовать для объявления маркерного типа.)
Это ключевое слово означает, что для каждого элемента атрибуту должно быть присвоено уникальное значение.
(Например, присвоение товарного кода «S021» двум элементам ITEM не допускается.) -->

<!DOCTYPE INVENTORY[
	<!ELEMENT INVENTORY (ITEM*)>
	<!ELEMENT ITEM (#PCDATA)>
	<!ATTLIST ITEM StockCode ID #REQUIRED>
]>

<INVENTORY>
	<!--Каждый элемент ITEM должен иметь свое значение кода StockCode-->
	<ITEM StockCode="S021">Персиковый чай</ITEM>
	<ITEM StockCode="S034">Электрокофемолка</ITEM>
	<ITEM StockCode="S086">Термометр</ITEM>
</INVENTORY>

<!-- Список ключевых слов, которые можно использовать в определении маркерных типов атрибутов,
и ограничения, которые они накладывают на значения атрибутов:

ID. Для каждого элемента атрибут должен иметь уникальное значение.
Данный тип элемента может иметь только один атрибут типа ID,
а в объявлении значения атрибута по умолчанию должно фигурировать #REQUIRED или #IMPLIED.

---------------------------------------------------------------------------------------------------

IDREF. Значение атрибута должно совпадать со значением атрибута элемента типа ID внутри документа.
Другими словами, этот тип атрибута является ссылкой на уникальный идентификатор другого атрибута.
Например, можно добавить атрибут IDREF с именем GoesWith к элементу ITEM:

<!ELEMENT ITEM (#PCDATA)>
<!ATTLIST ITEM StockCode ID #REQUIRED GoesWith IDREF #IMPLIED>
Далее можно использовать этот атрибут для ссылки на другой элемент ITEM:

<IТЕМ StockCode="S034">Электрофемолка</IТЕМ>
<IТЕМ StockCode="S047" GoesWith="S034">Щётка</IТЕМ>

---------------------------------------------------------------------------------------------------

ENTITY. Значение атрибута должно совпадать с именем примитива, объявленного в DTD.
Этот примитив не обрабатывается синтаксическим анализатором и ссылается на внешний файл,
обычно содержащий не XML-данные.
Например, в DTD вы объявляете элемент с именем IMAGE, представляющий графическое изображение,
и атрибут типа ENTITY с именем Source, указывающий на источник графических данных:

<!ELEMENT IMAGE EMPTY>
<ATTLIST IMAGE Source ENTITY #REQUIRED>

---------------------------------------------------------------------------------------------------

ENTITIES. Этот тип атрибута похож на тип ENTITY, за исключением того, что значение может содержать
имена нескольких не анализируемых примитивов - разделенных пробелами - внутри строки в кавычках.
Например, при назначении атрибуту Source тип ENTITIES следующим образом:

<!ELEMENT IMAGE EMPTY>
<!ATTLIST IMAGE Source ENTITIES #REQUIRED>
его можно использовать для ссылки на несколько не анализируемых примитивов
(допустим, примитивов, содержащих графические данные в альтернативных форматах), например, так:

<IMAGE Source-"LogoGif LogoBmp" />

(Здесь подразумевается, что LogoGif и LogoBmp - имена не анализируемых примитивов, которые были объявлены в DTD.

---------------------------------------------------------------------------------------------------

NMTOKEN. Это значение есть элементарное имя (name token), представляющее собой имя,
состоящее из одной или более букв, цифр, точек (.), тире (-) или символов подчеркивания (_).
Элементарное имя может также содержать двоеточие (:), но не на первом месте.
Например, при назначении атрибуту ISBN типа NMTOKEN следующим образом:

<!ELEMENT BOOK (#PCDATA)>
<!ATTLIST BOOK ISBN NMTOKEN #REQUIRED>
ему можно присвоить значение, начинающееся с цифры

<ВООК isbn="9-99999-999-9">Рисунок человека<ВООК>

---------------------------------------------------------------------------------------------------

NMTOKENS. Этот тип атрибута похож на тип NMTOKEN, но значение может содержать несколько элементарных имен,
разделенных пробелами, внутри строки в кавычках.
Например, если назначить атрибуту Codes тип NMTOKENS следующим образом:

<!ELEMENT SHIRT (#PCDATA)>
<!ATTLIST SHIRT Codes NMTOKENS #REQUIRED>
То ему можно присвоить несколько значений в виде элементарных имен:

<SHIRTCodes="38 21 97">long sleeve Henley</SHIRT> -->